services:
  notify-fe-headless:
    image: ghcr.io/wiesner-philipp/error-pages:latest
    container_name: notify-fe-headless
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: 
      - .env